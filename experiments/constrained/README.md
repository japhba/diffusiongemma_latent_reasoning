# constrained/ — constrained-text task battery

`battery.py`: generators + exact verifiers for the constrained-text tasks (univocalic,
lipogram, piem, self-count, palindrome-words, ...) — the task source for the ember /
palindrome studies (`planning/constrained_common.py`) and for `lockin/ds_battery.py`.
